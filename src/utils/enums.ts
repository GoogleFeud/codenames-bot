

export const enum ARGUMENT_TYPES {
    SUB_COMMAND = 1,
    SUB_COMMAND_GROUP,
    STRING,
    INTEGER,
    BOOLEAN,
    USER,
    CHANNEL,
    ROLE
}

export const enum WORD_TYPES {
    RED,
    BLUE,
    NEUTRAL,
    ASSASSIN
}

export const enum TEAMS {
    RED,
    BLUE
}